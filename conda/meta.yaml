package:
    name: dss_python
    version: "0.10.3a1"
    
source:
    #git_rev: "0.10.2"
    #git_url: https://github.com/dss-extensions/dss_python.git
    path: ..
    
build:
    script_env:
        - DSS_CAPI_PATH

requirements:
    build:
        - {{ compiler('c') }} # [linux]
        - {{ compiler('cxx') }} # [linux]
        - python {{ python }}
        - cffi
        - setuptools
        - wheel
        - cmake [osx]
        # - git
        
    host:
        - python
        - setuptools
        - cffi
        
    run: 
        - python
        - cffi
        - numpy

